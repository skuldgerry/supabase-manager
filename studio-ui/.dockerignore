# Root folder of this turbo monorepo
# Doesn't send useless context to Docker daemon
.git
.github
about
docker
# Un-ignore the compose template baked into the Studio image
!docker/docker-compose.yml
!docker/volumes
# Exclude postgres data directory (owned by postgres, not readable by build user)
docker/volumes/db
examples
i18n
**/node_modules
apps/docs/spec
supabase
tests

**/.next
.vercel
**/.env.*

.dockerignore
apps/studio/Dockerfile
